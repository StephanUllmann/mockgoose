name: Publish

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Install Dependencies
        run: npm install
      - name: Publish to JSR
        uses: actions/checkout@v6
        run: npx jsr publish
