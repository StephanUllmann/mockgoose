{
  "name": "@stephanullmann/mockgoose",
  "version": "0.0.4",
  "description": "A mock of the Mongoose API",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "mongoose": "^9.0.0",
    "typescript": "^6.0.0"
  },
  "peerDependencies": {
    "mongoose": ">=6"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/StephanUllmann/mockgoose.git"
  }
}
